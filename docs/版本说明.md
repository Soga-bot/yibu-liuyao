# 版本说明（技术文档）

> **约定**：每个推送版本一条记录——本版实现了什么、验证如何、下一版要改什么、
> 与哪些版本互为前置/后置。逐改动的实况细账见 [开发日志.md](开发日志.md)（编号 ①–㊹），
> 长期路线与立案见 [优化方向.md](优化方向.md)。
> 版本号与应用内「关于」页显示的版本号（pages/mine/mine.js `VERSION`）同步。
> 回退方式：`git checkout vX.Y.Z`（或 `git reset --hard vX.Y.Z` 后强推，慎用）。

---

## v0.3.11（当前版本 · 2026-08-26 · tag `v0.3.11`）

**定位**：tab 页标题修复迁移轮——用户确认解卦页 6.4vh 到位，授权迁移典籍库/
我的，并要求「导航栏所在高度不透明」，内容不从栏后透出。

**前置**：v0.3.10。

**本版实现**：dianji/mine 追加与解卦同款定位块（结构与解卦字节级同构）；
tab 三页（解卦/典籍库/我的）顶栏统一加实底 `background: var(--bg)`——此前
tab 三页顶栏透明（仅 border-bottom），内容异常上溢时直接压在标题上；同固定
顶栏 3 页（卦详情/设置/十翼）既有做法，实底随 CSS 变量深浅翻转。固定 3 页
本就带实底，未动。

**验证**：三页定位块/实底就位、花括号配平、引擎 22/22；仅 wxss+js 版本号+
文档改动，热重载生效。

**已知风险**：顶栏实底遮住页角径向渐变（tint-05 五透明度，近乎不可见，无碍）；
双下限保护仍欠（js px 绑定回归待做）。

**后置**：双下限 js 回归 → v0.4.0（AI 后端、BGM 音源等）。

---

## v0.3.10（2026-08-26 · tag `v0.3.10`）

**定位**：tab 页标题修复先行轮——用户确认解卦/典籍库/我的三页标题同被严重遮挡
（同五行 v0.3.4 病：spacer 被压、锚点1 ≈3.8% 屏高），五行等 8 页已修。用户复述
五行同款校准链（钉正中 → 上移 5/6 → 再上移 2/3），锚点1 = 被压位时终点恰为
6.4vh，故直接对齐、不再中间钉正中；本轮**仅解卦页**，确认后迁移典籍库/我的。

**前置**：v0.3.9。

**本版实现**：paigua.wxss 追加五行同款定位块——`.status-bar { display: none; }`
+ `.topbar { margin-top: calc(6.4vh - 44rpx); }`（顶栏已有 `flex-shrink: 0`，
margin 不参与 flex 收缩，定位确定）。仅 wxss 改动，热重载即生效，不动 wxml/js。

**验证**：paigua 定位块就位、wxml/js 零改动；其余页面未动。

**已知风险**：tab 页底部 tabbar 让位垫块（170rpx+安全区）不受影响（scroll-view
flex:1 自适应）；典籍库/我的仍未修（下一轮迁移）。

**后置**：用户真机确认解卦位 → 迁移典籍库/我的 → 双下限 js 回归。

---

## v0.3.9（2026-08-26 · tag `v0.3.9`）

**定位**：编译排障轮——用户报 v0.3.8「开发者工具编译不出来」。全量体检：16 个
wxml 标签/引号/插值配平（以未改动页面为基线自证检查器）、9 wxss 花括号配平，
结构无异常；v0.3.8 唯一新语法 = 顶栏内联 `max(calc(...), {{statusBarHeight}}px)`
（wxss 纯 calc 自 v0.3.5 起一直可编译）。另本机代理 7897 上游断链（git 推送
同源失败），工具若走系统代理亦可能卡「编译中」。

**前置**：v0.3.8。

**本版实现**：拆除 9 页内联 `max()`——在流 6 页回纯 `<view class="topbar">`
（定位全靠 wxss `calc(6.4vh − 44rpx)`，观感不变）；固定 3 页顶距/让位改纯
calc（`calc(6.4vh − 44rpx)` / `calc(6.4vh + 44rpx)`，无插值）。状态栏双下限
保护暂缓，待编译恢复确认根因后换 js 计算 px 绑定回归。

**验证**：脚本 15 断言全中；全站 wxml `max(` 清零；wxss calc 规则仍恰 6；
花括号配平；引擎 22/22。

**已知风险**：若拆 max 后仍编译失败，根因在工具端/网络代理（需报错原文定位）；
刘海屏机型双下限保护暂缺（js 回归时补）。

**后置**：编译恢复确认 → 双下限 js 回归 + tab 页 spacer 加固（待用户指示）→ v0.4.0。

---

## v0.3.8（2026-08-26 · tag `v0.3.8`）

**定位**：标题定位迁移轮——用户确认五行页 6.4vh「刚刚好」，按约定迁移其余
8 页，并加状态栏双下限保险（v0.3.7 承诺项）。

**前置**：v0.3.7。

**本版实现**：

1. **在流 6 页**（五行/八卦/八宫/排盘/卦成/问易）：wxss 同款定位块（spacer 藏 +
   `margin-top: calc(6.4vh − 44rpx)` + `flex-shrink: 0`）；wxml 顶栏内联
   `margin-top: max(calc(6.4vh − 44rpx), {{statusBarHeight}}px)`——高状态栏
   机型（刘海屏 ≈5.6% 屏高）回落自然位（状态栏 + 88rpx），用户机型（状态栏
   5.0%）vh 档生效、观感不变；wxss calc 为旧基础库回退层。
2. **固定顶栏 3 页**（卦详情/设置/十翼）：顶距 `{{statusBarHeight}}px` →
   `max(calc(6.4vh − 44rpx), {{statusBarHeight}}px)`，页身让位
   `calc(状态栏px + 88rpx)` → `max(calc(6.4vh + 44rpx), calc(状态栏px + 88rpx))`。

**验证**：批量脚本 14 断言全命中；wxss `calc(6.4vh − 44rpx)` 规则恰 6、wxml
内联 max 恰 6、固定三页顶距/让位 max 恰 3+3；9 wxss 花括号配平；js 未动
（mine.js 仅版本号）；引擎 22/22。真机矩阵仍未跑。

**已知风险**：`max()` 需 iOS 12.2+ / Chromium 79+（微信现行基础库均满足）；
tab 三页（解卦/典籍库/我的）未动，其 spacer 无 flex-shrink 保护（「我的」
观感疑点留待用户指示后再加固）。

**后置**：v0.4.0（AI 后端 / BGM / 真机回归 / 发布准备，计划不变）。

---

## v0.3.7（2026-08-26 · tag `v0.3.7`）

**定位**：标题定位校准第二档（仍仅五行页）——用户实测 v0.3.6 的 11.5vh 偏低，
给分值「上移 = 之前锚点（被摄像头遮挡处 ≈3.8% 屏高）到当前锚点（11.5vh）距离
的 2/3」：11.5% − 2/3×7.7% = **6.4vh**。同轮以 git 取证澄清「我的」页标题
两轮未动（v0.3.4→v0.3.6 仅 mine.js 版本号字符串变更），用户观感差异系构建
新旧或其占位块受压面，留待加固轮处理。

**前置**：v0.3.6。

**本版实现**：五行页 `calc(11.5vh − 44rpx)` → `calc(6.4vh − 44rpx)`，机制不变
（margin 定位不受 flex 收缩 + `flex-shrink: 0` + spacer 藏）。

**验证**：wxss 花括号配平；`6.4vh` 规则恰 1 处、`11.5vh` 规则清零（注释过程
记述除外）；mine.js `node --check` 过；引擎 22/22。真机矩阵仍未跑。

**已知风险**：6.4vh 字顶距状态栏底仅约 3px（用户机型：状态栏 5.0% 屏高）——
偏紧；纯 vh 定位在更高状态栏机型（如刘海屏 ≈5.6%）字顶会侵入状态栏。迁移轮
拟加 `max(vh 档, 状态栏 px)` 双下限保护。

**后置**：v0.3.8（用户确认档位后迁移其余 8 页 + 双下限保护 + tab 页 spacer 加固）。

---

## v0.3.6（2026-08-26 · tag `v0.3.6`）

**定位**：标题定位定稿轮（**单页先行**）——v0.3.5 校准（顶栏钉 50vh 正中）真机
目测成功，用户回报分值「上移 = 之前锚点（被摄像头遮挡处 ≈3.8% 屏高）到正中
距离的 5/6」：50% − 5/6×(50% − 3.8%) = **11.5vh**，即标题栏中心定于屏高 11.5%
（比标准导航位 7.7% 略低，用户所求的空气层）。按用户指示本轮**仅改五行页**
验证效果，通过后迁移其余 8 页。

**前置**：v0.3.5。

**本版实现**：五行页校准块（50vh）换定稿块（11.5vh），机制不变——spacer 藏、
`.topbar` `margin-top: calc(11.5vh − 44rpx)` + `flex-shrink: 0`（margin 不参与
flex 收缩、栏体禁缩，定位确定性；44rpx = 88rpx 栏高之半，故栏中心恰落 11.5vh）。

**验证**：wuxing.wxss 花括号配平；`11.5vh` 恰 1 处、`50vh`/校准临时块标记清零；
mine.js `node --check` 过；引擎 22/22（复跑确认）。真机矩阵仍未跑。

**已知风险**：11.5vh 为该用户机型校准值，其他机型观感待真机矩阵复核（若需
微调只动一个数字）；‹ 返回箭头与内容让位需在本页一并复核。

**后置**：v0.3.7（验证通过后迁移其余 8 页：在流 5 页同法定稿，固定顶栏 3 页
内联顶距与让位同步 11.5vh）。

---

## v0.3.5（2026-08-26 · tag `v0.3.5`）

**定位**：标题定位校准轮（**临时，仅五行一页**）——v0.3.4 真机复测：标题回来了，
但骑进状态栏、被前置摄像头遮挡。像素取证（截图 633×1371 逐行扫描）：状态栏带
y 0–69，「五行基础」字形 y≈35–69、中心 y≈52（屏高 3.8%；正确应在 y≈106 =
7.7%）——status-bar 占位在真机被压至 ≈0–20px。同注入同写法的「我的」页正常，
其 `.topbar` 独有 `flex-shrink: 0`（五行页 spacer 与栏体均可缩），flex 收缩
列为头号疑点，待校准分值回来后定案。用户指示：单页验证成功再迁移其余页。

**前置**：v0.3.4。

**本版实现**：五行页 wxss 加「校准临时块」：`.status-bar` 隐藏、`.topbar` 以
`margin-top: calc(50vh - 44rpx)` 定位并 `flex-shrink: 0`——标题栏中心恰落
**屏幕正中 50vh**；margin 不参与 flex 收缩、栏体禁缩，定位在真机上确定性。
用户以正中为参照目测回报「上移几分之几屏」，下一版换算为最终参数。

**验证**：wuxing.wxss 花括号配平；校准块 grep 命中、全站仅此一处 `display: none`
spacer；mine.js `node --check` 过；引擎 22/22（未动，复跑确认）。真机矩阵仍未跑。

**已知风险**：临时版，定稿时校准块须整块删除；`calc(vh − rpx)` 混算若个别机型
不识别，标题会贴顶（用户回报即辨）；‹ 返回箭头与标题同行随栏移动，正中状态下
一并复核。

**后置**：v0.3.6（按分值定稿标题位置 + 迁移其余 8 页 + 根因加固 flex-shrink）。

---

## v0.3.4（2026-08-26 · tag `v0.3.4`）

**定位**：顶栏机制归一——用户报 v0.3.3 编译后「之前有问题的几个页面完全看不到
标题」。静态核查（wxml 结构 / 全部 topbar 规则 / 全局 box-sizing / skyline 渲染器
/ 13 页 js 注入链）均无异常；唯一可疑面是 v0.3.3 新引入的「顶栏显式 height +
padding 布局」——全项目唯一未在用户设备验证过的机制。

**前置**：v0.3.3。

**本版实现**：9 个二级页（五行/八卦/八宫/排盘/卦成/问易/卦详情/设置/十翼）顶栏
**去掉显式栏高**，改由子元素 `.back`/`.title` 的 `height + line-height: 88rpx`
撑起——与「我的」页标题行（用户设备上显示正常）完全同一机制，auto 高度对
box-sizing 差异天然免疫；`.back` 恢复 `text-align: center`。视觉规格不变
（状态栏 + 88rpx、52rpx 裸箭头、38rpx 标题、让位 calc 同步）。

**验证**：批量替换 10 处断言全命中；`.topbar` 栏体显式 height 清零
（`line-height: 88rpx` 12 文件 = 9 页子元素 + 3 tab 页标题行，恰合预期）；
9 wxss 花括号配平；引擎 22/22。真机矩阵仍未跑。

**已知风险**：若用户端清缓存重启工具重编译后标题仍消失，则排除代码层，
转工具端缓存/基础库问题（需截图定位）。

**后置**：v0.4.0（AI 后端 / BGM / 真机回归 / 发布准备，计划不变）。

---

## v0.3.3（2026-08-26 · tag `v0.3.3`）

**定位**：顶栏观感统一轮——用户看图复测（五行/八卦「依旧太高、顶天」，「我的」
标题高度为唯一认可参照；典籍库/解卦报标题顶天 + 底部导航仍遮挡）。

**前置**：v0.3.2。

**取证**：五张截图经视觉模型核对，圆钮直径 ≈35px 对应 64rpx（v0.2.0 尺寸，
v0.3.1 已收至 56rpx）；git 考古 v0.2.0 起 tab 页让位一直是 140rpx——**截图对应
v0.3.1 之前的构建**，v0.3.1/v0.3.2 两轮改版均需开发者工具整编后才见效。取证同时
暴露两处与构建新旧无关的真问题，本版修掉：

1. **二级页顶栏全面对齐「我的」节奏**：9 页（五行/八卦/八宫/排盘/卦成/问易/
   卦详情/设置/十翼）栏体统一 **状态栏 + 88rpx**（与 tab 三页认可高度一致）；
   **实底圆钮换裸箭头**（64rpx 宽、52rpx 细字 ‹、上下 16rpx 点击衬垫、垂直居中
   ——十翼页既有范式推广）。「顶天」根因是实底圆钮上缘距状态栏仅 8rpx 视觉挤死；
   裸箭头居中于 88rpx 栏内，墨迹距顶约 24rpx，与「我的」标题同一空气层。固定
   顶栏两页（卦详情/设置）内联顶距简化为「状态栏 px」、页身让位 72 → 88rpx。
2. **tab 三页底部让位改实体垫块**：解卦/典籍库/我的的 tabbar 让位由 scroll-view
   自带 `padding-bottom: 140rpx` 改为滚动内容尾部 `<view class="tab-clear">`
   （`calc(170rpx + env(safe-area-inset-bottom))`）——scroll-view 底 padding
   在部分机型会截短滚动范围（末条压进 tabbar），实体垫块免疫该怪癖，并补上
   全面屏 Home 条安全区。

**验证**：批量替换 22 处断言全命中；旧标记 grep 清零；12 wxss 花括号配平、
6 wxml 标签配平；`height:88rpx` 全站 12 处恰合预期；mine.js `node --check`；
引擎 22/22。真机矩阵仍未跑。

**已知风险**：裸箭头无实底，深色下对比度依赖 var(--text-2)（与十翼页同源）；
返回键点按热区约 32×42px（原圆钮 28×28px，实际变大）。

**后置**：v0.4.0（AI 后端 / BGM / 真机回归 / 发布准备，计划不变）。

---

## v0.3.2（2026-08-26 · tag `v0.3.2`）

**定位**：用户实测六项反馈修复轮（导航遮挡根治 / 全站更名 / 成卦字号 / 问易措辞 /
合参呈现补档 / 设置三态提示）。

**前置**：v0.3.1。

**本版实现**：

1. **tab 三页导航遮挡根治**：解卦 / 典籍库 / 我的由「fixed 顶栏 + calc 让位」改为
   **文档流顶栏 + scroll-view 内滚**（与五行 / 八卦 / 排盘等 7 页同范式）——内容
   在滚动容器内滚，物理上不可能钻到顶栏底下；tabbar 让位 140rpx 挪进滚动内容
   底 padding。此前 fixed 方案在用户环境仍报遮挡（代码侧结构与「我的」逐项一致、
   未定位出差异，直接换结构性免疫的范式）。
2. **全站更名 易卜六爻 → 易研六爻**：13 文件——app.json 分享基名、sitemap、
   project.config 描述、README、技术文档 / 优化方向 / 开发日志标题、分享卡落款
   （sharecard.js）、首页大字与分享标题（index）、detail / result 分享标题、
   「我的」关于文案。GitHub 仓库名 `yibu-liuyao` 未动（远端重命名需用户操作）。
3. **成卦页正文字号接入三档**：卦辞 / 彖象 / 白话 / 爻辞 4 条规则接
   `calc(Xrpx * var(--fss,1))`，设置页说明同步加「成卦」。
4. **问易措辞去直白**：卦成页主按钮「问易 · AI 解卦」→「问易 · 合参此卦」；
   问易页卡头→「问易解读」、预告文案与页脚去「AI」字样（审核口径更稳）。
5. **合参呈现方式补档**（[问易解读规格.md](问易解读规格.md) 第六节）：整篇合参
   解读为主体，本 / 变卦经文不在解读内重复——卦成页两框分列 + 典籍详情即
   「整合讲解 + 单独读本卦、变卦」，现有页面已承载。
6. **设置页深色三态提示**：「跟随系统」下加当前系统外观说明（系统为浅色时与
   「浅色」效果相同，差别在系统切深色时显现），sysTheme 注入 onLoad/onShow。

**验证**：三页 wxml view/scroll-view 配平、4 wxss 花括号配平、settings.js
`node --check`、引擎 22/22、改名残留 0。真机矩阵仍未跑。

**已知风险**：卦详情 / 十翼 / 设置三页仍为固定顶栏 + calc 让位（本版未转
scroll-view），若真机仍见遮挡按同法转换；「合参此卦」措辞待用户过目。

**后置**：v0.4.0（AI 后端 / BGM / 真机回归 / 发布准备，计划不变）。

---

## v0.3.1（2026-08-26 · tag `v0.3.1`）

**定位**：顶栏高度收紧——用户实测反馈二级页标题栏与返回键偏高
（参照系：「我的」页 88rpx 标题栏、摇卦页悬浮返回键「状态栏 + 8px」节奏）。

**前置**：v0.3.0。

**本版实现**：

1. **二级页顶栏统一收矮**：`.topbar` 上下留白 16/8 → 8/8rpx（栏体 88 → 72rpx），
   返回圆钮 64 → 56rpx（字形 48 → 38rpx），标题右让位 64 → 56rpx 跟随（光学居中）。
   覆盖 10 页：五行 / 八卦 / 八宫 / 排盘 / 卦成 / 问易 / 卦详情 / 设置 / 十翼；
   固定顶栏三页（卦详情 / 设置 / 十翼）的 wxml 让位 calc 同步 88 → 72rpx。
   tab 三页（解卦 / 典籍库 / 我的）88rpx 纯标题栏为用户认可高度，不动。
2. **问事页返回键对齐摇卦页节奏**：顶距 `env(safe-area-inset-top) + 16rpx` →
   内联 `calc(状态栏px + 8rpx)`（env() 部分真机失效，改走 JS px 值，与摇卦页
   cover-view 同法；ask.js 补 `statusBarHeight` 注入），圆钮同步 56rpx。

**验证**：grep 残留清零（旧 padding / 64rpx 顶栏圆钮无存留，88rpx 仅存 tab 三页
标题栏）；ask.js `node --check` 过；纯样式改动，引擎与数据未动。真机矩阵仍未跑。

**已知风险**：返回键触达面积缩小约 12%（56rpx ≈ 28px@375，仍在自定义返回键
常规范围），真机点按手感待回归确认（清单 §七-9）。

**后置**：v0.4.0（AI 后端 / BGM / 真机回归 / 发布准备，计划不变）。

---

## v0.3.0（2026-08-26 · tag `v0.3.0`）

**定位**：体验修复轮——用户实测反馈四项（导航遮挡 / 问易回看断链 / 本变合参 / 设置缺失）。

**前置**：v0.2.0（第 4 项依赖其 CSS 变量体系；第 1 项复用其边线/阴影变量）。

**本版实现**：

1. **纯色固定顶栏**：解卦 / 典籍库 / 我的（tab 页）+ 卦详情 / 十翼通读（长正文页，带
   返回键）。`position:fixed` + `var(--bg)` 不透明 + 底边细金线；内容以
   `calc(状态栏px + 88rpx)` 让位（rpx 混算，宽屏精确）；其余长页（排盘/结果/五行/
   八卦/八宫/问易）本就 `scroll-view` 内滚，顶栏不可能被滚上去，不动。
2. **问易按钮分态 + 挂靠修复**：根因在 wenyi.js 只在「无参 + id」时才查历史条目——
   从卦成页**带全参**进问易时 `_entry` 恒空，已存解读从不加载（即用户所见断链）。
   改为「有 id 即挂靠」；result 主按钮已解读 → 「回看 · 问易解读」，onShow 返回即
   刷新；去重合并进旧条目（解读挂旧 id）的情形一并覆盖。
3. **本变合参规格立档** [问易解读规格.md](问易解读规格.md)：四段结构
   「本卦现状 → 动爻之几 → 变卦趋向 → 合参所问」、payload 契约（补 name/bian/
   bianName）、审核口径与不杜撰约束；askAI payload 已按规格带全，实施待 AI 后端。
4. **独立设置页** `pages/settings`（自「我的」进入）：
   - 深色模式三态：跟随系统 / 浅色 / 深（storage `ly_theme`；`.t-light`/`.t-dark`
     覆盖类挂各页根节点，app.wxss 声明全套变量；TabBar 图标 + 消费变量联动，
     `pageLifetimes.show` 返回刷新；**divination 3D 页不挂类保持浅色**）；
   - 阅读正文三档：标准 / 大 / 特大（`--fss` 缩放系数 1 / 1.15 / 1.3，正文写
     `calc(Xrpx * var(--fss,1))` 层级不压扁；作用于卦详情 / 十翼 / 问易正文 +
     设置页预览行；界面其余字号不变）；
   - 背景音乐卡自「我的」迁入（行为不变：预留态无声、手动点音符开启）；
   - 14 页根节点接覆盖类；4 页状态栏 spacer 移入 `.page` 内，消除手动主题顶部接缝。

**验证**：临时脚本 310 断言（theme 三态 / 设置页 / mine / result 分态 / wenyi 挂靠 /
全页冒烟 / wxml 配平 / wxss 扫描）+ 引擎 22/22；真机矩阵仍未跑（清单新增第七节）。

**已知风险**：手动主题下 iOS 过度回弹瞬间可能露出系统色（page 元素背景无法逐页
覆盖，接受）；二级页首帧极端时序理论上可闪一帧（onLoad 内 setData 已合并进首渲染）。

---

## v0.4.0（计划中 · 未动工）

**前置**：v0.3.4。

1. AI 后端接入：按 [问易解读规格.md](问易解读规格.md) 实现服务端，填 wenyi.js
   `AI_API`（密钥不下发端上）；
2. BGM 音源配置：填 utils/bgm.js `BGM_SRC`（包内路径或 CDN）；
3. 真机回归：清单五 / 六 / 七节，跑完填机型矩阵；
4. 发布准备（P3-12）：正式 AppID / 提审 / 隐私声明。

---

## v0.2.0（2026-08-26 · commit `166a804`）

**定位**：体验层收官轮——P2-9 分享与深色完成 + 背景音乐接口预留。

**前置**：v0.1.0（`825f93d` 基线）。

**本版实现**：

1. **packOptions 忽略 docs/**（`1d56756`）：校对工作 txt 不再入包，主包约 -292KB。
2. **深色模式「墨色宣纸」**（`cb8fcad`）：
   - app.json 开 `darkmode` + `themeLocation`，新增 theme.json（window 双主题）；
   - app.wxss 定义 40 个 page 级语义变量，`prefers-color-scheme: dark` 整体翻转；
   - 15 个页面 wxss 共 431 处硬编码色替换为变量，浅色视觉零变化；
   - 例外保持：红底按钮烫金字、铜钱印章插画、五行彩圆；**divination 3D 页整体钉死浅色**；
   - TabBar：底色/文字走变量继承；图标 SVG 描边色在 data-URI 内，JS（theme + onThemeChange）
     加 `dark` 类切换 8 条提亮图标。
3. **卦象分享卡 + 全站转发**（`3de5a42`）：
   - utils/sharecard.js：2D canvas 宣纸风 5:4 卡（六爻/卦名/全称/一句话白话），固定浅色；
   - 隐藏画布仅 result 与 dianji/detail（屏外定位；divination 页禁用，webgl 同层渲染教训）；
   - result：`from=share` 回放守卫（不落对方历史）+ 起卦行三态标签 + 「分享此卦」按钮；
   - detail：转发落地 `detail?key=` + 朋友圈 query 带 key；
   - 七个内容页轻量 onShareAppMessage（静态知识页加朋友圈）；个人/流程页不定义转发；
   - 标题全文化口径，零占断措辞（审核红线）。
4. **背景音乐预留接口**（`166a804`）：
   - utils/bgm.js：InnerAudioContext 全局单例（loop / obeyMuteSwitch / volume 0.22），
     跨页不断播；app.json 声明 `requiredBackgroundModes:["audio"]`；
   - **预留态**：BGM_SRC 留空 = 未配置，点音符仅提示绝不发声（与 AI_API 同范式）；
   - 「我的」页背景音乐卡 + 音符按钮（唯一出声入口，守微信「禁自动播放」规范）；
   - 开关按播放态驱动，冷启动默认亮但无声。

**验证**：临时导入脚本 39 + 11 断言全过；引擎 22/22；wxss 非 ASCII 选择器 0、
花括号配平、var 引用完整。真机矩阵仍未跑（见 [真机回归清单.md](真机回归清单.md)
第五/六节为本版新增检查项）。

**已知风险**：iOS 真机 InnerAudioContext 后台续播（清单五-4）；深色真机观感未验。

**打版说明**：功能代码基点为 `166a804`；本记录提交紧随其后（仅文档 + 版本号），
git tag `v0.2.0` 落在含本文档的记录提交上，回退即回退到该 tag。

---

## v0.1.0（基线 · 2026-08-26 及之前 · commit `825f93d`）

**定位**：主链路与内容层全量完成。

- 3D 摇卦闭环（龟壳铜钱/手势拨动）→ 排盘 → 卦成 → 历史（ly_history，id 锚定）；
- 典籍库：64 卦经文 + 十翼（彖/象/文言/序/杂/系辞/说卦）+ 白话经传 + 典故 46 + 章旨，
  内容管线全人工校对闭环，载入断言体系（含京房八宫 64 卦全表推导断言）；
- 知识页：解卦术语速查 / 五行基础 / 八卦基础 / 卦象速查；
- 问易（AI 解卦壳页）：AI_API 留空预告态，解读落库/回看管线就绪；
- 历史回看直达只读卦成页；一事一占软提示（蒙卦辞）；
- 摇卦音效、问事签、免责声明体系（审核口径全站贯彻）。

细账见开发日志 ①–㊸。
