# 真机回归清单（3D 摇卦主链路）

> 生成：2026-08-25。开发者工具全绿≠真机全绿，以下三项此前只在工具里验过。
> 预计一轮 30–45 分钟。建议机型：iOS 一台 + 安卓一台（最好含一台低端机，
> 如骁龙 660 级 / 3GB 内存）——WebP 与离屏画布的兼容差异都出在低端安卓。

## 机型矩阵（跑完填）

| 机型 | 系统 | 微信版本 | 基础库 | 走查人 | 日期 | 结果 |
| --- | --- | --- | --- | --- | --- | --- |
|  |  |  |  |  |  |  |

---

## 一、摇卦实时音效（v0.3.13 命中驱动；停止判定 v0.3.15 回摆固定）

**代码事实**：摇动音效**命中驱动**——每次有效晃动（`addShakeHit`）即播一记
钱壳碰撞短音（get_into.mp3 池 ×3，音量随加速度强度 0.35~0.85），旧 process.mp3
循环已撤（接缝/起播延迟/停顿重起三类问题连根除，文件保留未删）。停止判定：
末次命中后静默**固定 1300ms** 即倒出（v0.3.13 曾试节奏自适应阈值——前两下
快摇就把阈值压到 ~0.7s、换气即倒，实测手感差，v0.3.15 回摆）；摇动中按钮
置灰「摇动中…」，点按=补一记晃动（工具端兜底）。

**步骤**：进入摇卦 → 装钱 → 持续摇 10 秒以上（中途换气几拍）→ 停手 → 倒出；
再试只摇两三下即停一轮。

| # | 检查点 | 判定标准 |
| --- | --- | --- |
| 1 | 一晃一响 | 每次晃动同步出声、无起播延迟感；重摇响、轻摇轻（音量随强度） |
| 2 | 停止判定 | 停手 ~1.45s 内开始倒出（1.3s 静默 + 前倾）；连摇 10s+ 中途换气 0.8~1s 绝不误倒 |
| 3 | 短摇可倒 | 只摇两三下就停，静默 1.3s 后照常倒出成爻 |
| 4 | 短音不破音 | 快摇连击 ~3Hz 时碰撞短音不哑（池 ×3 够）；入壳/落地音不受影响 |
| 5 | 切后台/锁屏 | 摇动中切后台再回，无残留声、流程状态正常 |
| 6 | iOS vs 安卓 | 两端音量一致性、首响起播延迟是否可察觉 |

**若首响延迟明显**（池预热仍救不了首播解码）：进页静音预播一记 clink
（volume 0）完成解码预热，代码 3 行；碰撞音色若显单薄，换自备短音素材
同名替换 get_into.mp3 即可。

---

## 二、爻线雕刻与卦名外观

**代码事实**：卦名走 `wx.createOffscreenCanvas({type:'2d'})` 真离屏画布
（`divination.js:167-172`，失败则静默跳过不刻，不崩流程）；`carveGuaName()`
（`divination.js:796`）512×128 画布、`bold 96px sans-serif`、CanvasTexture 贴壳背
上爻上方。**本次已修**：八纯卦刻名原为「天天乾」式，已改「乾为天」式
（与典籍库列表同规则），真机需首次实际确认。

| # | 检查点 | 判定标准 |
| --- | --- | --- |
| 1 | 卦名出现 | 六摇成卦后卦名如约刻出（低端安卓重点：offscreenCanvas 失败会静默不刻，看 vConsole 有无「离屏画布创建失败」） |
| 2 | 八纯卦刻名 | 摇出乾/坤等八纯卦，刻名为「乾为天」而非「天天乾」（本次修复项） |
| 3 | 字体外观 | `sans-serif` 在两端字重宽度不同（iOS 苹方 / 安卓各厂默认），卦名不溢出、不压上爻线 |
| 4 | 清晰度 | 卦名与爻线纹理清晰，无糊化/锯齿刺眼（贴图 512×128，缩放 0.02） |
| 5 | 贴面贴合 | 卦名平面顺弧面倾斜，不同屏宽下不穿壳面、不悬空 |
| 6 | 亮名时机 | 卦成重震一下后卦名亮起（`divination.js:575`），时序不抢戏 |
| 7 | 连续多局 | 第二局起卦名/爻线无残留（画布跨局复用有 clearRect，实测确认） |

**若低端机不刻卦名**：降级方案为 WXML 藏 type=2d 节点方案（已知会诱发同层渲染
失效，见 `divination.js:163` 注释）——不采用；可改为卦成时页面上层 toast 式
卦名横幅兜底。

---

## 三、WebP→PNG 贴图（龟壳模型）

**代码事实**：`package3d/models/shell.glb`（899KB，分包）内嵌 **3 张 WebP 共 56KB**；
coin.glb 为 PNG（11KB，主包 `models/`）。旧基础库/部分安卓解码链对 GLB 内 WebP
支持不稳，风险是**壳贴图丢失**（纯色/黑壳/纹路缺失）。此前诊断过贴图体积很小，
即使全转 PNG 也只增约 100–200KB，可行。

| # | 检查点 | 判定标准 |
| --- | --- | --- |
| 1 | 壳外观 | 与开发者工具对照：纹路、色泽、细节齐全，无纯色块/丢失 |
| 2 | 低端安卓 | 重点机型上同样正常（WebP 解码失败多在此暴露） |
| 3 | 加载时长 | 冷启动进摇卦页可接受（壳 899KB + coin 11KB，分包按需拉） |
| 4 | 内存 | 连续 3 局无贴图异常（CanvasTexture 有 dispose，`divination.js:853`） |

**若贴图丢失**：gltf-transform（或 Blender 重导）将壳内 WebP 转 PNG 重打包，
`tools/reference/models-orig/` 有原模型备份；转完体积复核（分包红线 2MB，现
1585KB，余量够）。

---

## 四、顺带回归（同轮一并看）

| # | 项 | 说明 |
| --- | --- | --- |
| 1 | 开发者工具蒙层残影 | 工具端问题，真机若无则忽略；真机也有再立案 |
| 2 | 十翼通读页（新页） | 典籍库 → 十翼·通读，三章内容滚动、返回键正常（新页需完整编译后在真机预览） |
| 3 | 断法知识卡 | 解卦 tab 折叠卡展开/收起流畅，10 卡内容完整 |
| 4 | 历史记录 | 摇卦 → 排盘 → mine 出记录 → 回看不重复 |
| 5 | 免责声明可见 | 首页/我的页声明文案在（审核红线，别被样式改丢） |
| 6 | 主包体积 | 上传时看主包大小（coin 11KB + data 约 260KB，红线 2MB，应宽裕；docs 已从打包忽略，较上轮 -292KB） |
| 7 | 摇动默念提醒（v0.3.14） | 每爻摇动期按钮上方浮现「心中默念：『所问』」（未填所问则「心中默念所问之事」），呼吸式渐显、倒出即隐；长所问（30字）换行不遮按钮；问事签所问正确带入 |

## 五、背景音乐（预留接口，音源配置前后各查一轮）

| # | 项 | 说明 |
| --- | --- | --- |
| 1 | 预留态按钮 | 「我的」页背景音乐卡：音符按钮默认点亮态，点按 toast「音源尚未配置」，绝不出声 |
| 2 | 配置后手动开启 | 填 BGM_SRC 后：点音符出声，音量听感在 20–25% 档（不刺耳）；再点暂停 |
| 3 | 跨页不断播 | 开启后首页↔典籍↔摇卦来回跳，音乐不中断不重启 |
| 4 | 后台播放 | 声明 requiredBackgroundModes:["audio"]；切微信聊天/锁屏，**iOS 真机重点**（InnerAudioContext 后台续播是已知分险项，不稳则降级为仅前台播放并记录） |
| 5 | 硬件静音键 | iPhone 拨静音键后应无声（obeyMuteSwitch） |
| 6 | 冷启动不自动响 | 杀进程重进：开关仍亮但**无声**，须再点音符才出声（微信规范红线） |

## 六、深色模式与分享（2026-08-26 新增）

| # | 项 | 说明 |
| --- | --- | --- |
| 1 | 深色全站走查 | 系统切深色：14 页无「深底深字/浅底浅字」不可读块；重点 index/paipan/result/detail/mine |
| 2 | 3D 页例外 | divination 页保持浅色（深底龟壳铜钱看不清是刻意例外），顶栏文字仍深色可读 |
| 3 | TabBar 两态 | 图标随系统深浅切换（未选灰金/选中红），底色/文字同步 |
| 4 | 分享卡真机效果 | result/detail 转发：卡图卦象完整、楷体卦名无豆腐块（缺楷体回退衬线可接受）；对方点开 from=share 回放且不进其历史 |
| 5 | 朋友圈 | detail/shiyi/wuxing/bagua/bagong 可晒，query 直达对应卦/页 |

## 七、设置页与固定顶栏（2026-08-26 新增，v0.3.0）

| # | 项 | 说明 |
| --- | --- | --- |
| 1 | 固定顶栏遮蔽 | 解卦/典籍库/我的/卦详情/十翼：长内容从顶栏底下滚过，不再钻进状态栏/胶囊区域；重点看宽屏机型（414+）让位是否精确 |
| 2 | 顶栏返回键 | 卦详情/十翼固定顶栏返回键可点、不与胶囊重叠；tab 页无返回键 |
| 3 | 深色三态 | 设置页切「深色」全站即时换墨色（含 TabBar 图标）；系统深色下切「浅色」强制回浅；「跟随系统」恢复自动 |
| 4 | 3D 页例外 | 手动深色下进 3D 摇卦页仍为浅色（刻意见，顶部条可读） |
| 5 | 字号三档 | 切「大/特大」：卦详情/十翼/问易正文放大且层级不乱，界面其余字号不变；设置页预览行即时缩放 |
| 6 | 问易回看 | 已解读的卦（历史条目带「问易」标）：卦成页按钮显「回看 · 问易解读」，点进直接见已存文字；未解读仍「问易 · AI 解卦」 |
| 7 | 状态栏接缝 | 手动深色下五行/八卦/八宫/问易页顶部无浅色条（spacer 已移入 .page） |
| 8 | 设置返回 | 设置页改完主题/字号返回「我的」，tab 页即时生效（onShow 刷新） |
| 9 | 顶栏收矮（v0.3.1） | 二级页顶栏 72rpx：五行/八卦/八宫/排盘/卦成/问易/卦详情/设置/十翼标题与返回键高度统一、无错位；返回键 56rpx 点按顺手；问事页返回键不再用 env()（部分真机顶进状态栏的隐患已除）；tab 三页仍 88rpx |
| 10 | tab 页滚动重构（v0.3.2） | 解卦/典籍库/我的改 scroll-view 内滚：长列表滚动流畅、内容绝不钻顶栏底下、底部 tabbar 让位足（140rpx）、tab 快速切换无残影；另复核卦详情/十翼/设置三页固定顶栏有无同类遮挡（若有按同法转） |
| 11 | 顶栏对齐「我的」+ 实体让位（v0.3.3） | 9 个二级页（五行/八卦/八宫/排盘/卦成/问易/卦详情/设置/十翼）顶栏 = 状态栏+88rpx、裸箭头‹垂直居中无圆底，与「我的」标题同一高度节奏、不再「顶天」；tab 三页滚到最底，末条内容完整露出在 tabbar 之上（实体垫块 170rpx+安全区，重点全面屏 Home 条机型）；本轮动了 wxml，须整编后再验 |
| 12 | 顶栏机制归一（v0.3.4） | 9 个二级页顶栏标题与 ‹ 均正常显示（栏高由子元素 88rpx 行高撑起，与「我的」同机制，无显式栏高）；若清缓存+重启开发者工具后标题仍消失，截图定位工具端缓存/基础库问题 |
| 13 | 标题定位校准（v0.3.5·临时） | 仅五行页：真机复测标题骑进状态栏被摄像头遮挡（y≈35–69，spacer 被压至≈0–20px）；顶栏改 margin 钉屏幕正中 50vh（不受 flex 压缩），用户目测回报「上移几分之几屏」→ 下一版换算定稿、迁移 8 页、加固 flex-shrink；定稿时删校准块 |
| 14 | 标题定位定稿·单页先行（v0.3.6） | 仅五行页：校准分值换算栏中心 = 11.5vh（50% − 5/6×46.2%）；复核标题位置合意、‹ 返回箭头显示与点击、内容不与顶栏叠压、深色正常；通过后 v0.3.7 迁移其余 8 页（固定顶栏 3 页顶距与让位同步） |
| 15 | 校准第二档 6.4vh（v0.3.7·仍仅五行） | 用户报 11.5vh 偏低，分值「上移 = 之前锚点到当前锚点距离的 2/3」→ 6.4vh；重点看字顶与状态栏底的间隙（用户机型仅约 3px，偏紧）与 ‹ 显示；仍要微调则只动一个数字；确认后迁移轮加 max(vh, 状态栏px) 双下限 |
| 16 | 标题定位迁移（v0.3.8） | 8 页统一五行定稿 6.4vh（在流 5 页同款块 + 固定 3 页顶距/让位同步）；全部内联 max(vh 档, 状态栏px) 双下限；重点看各页标题与 ‹ 位置一致、固定 3 页让位无叠压、刘海屏机型回落自然位；tab 三页未动（「我的」spacer 保护待用户指示） |
| 17 | 编译排障（v0.3.9） | v0.3.8 编译不出来：16 wxml 结构体检全过（基线自证），拆 9 页内联 max() 回纯 calc（位置不变）后重试整编；若仍失败→查工具端/系统代理（本机 7897 上游断）并取报错原文；双下限保护待 js 绑定回归 |
| 18 | tab 页标题修复·先行（v0.3.10·仅解卦） | 解卦页标题应与其余 8 页同高（栏中心 6.4vh），不再被摄像头遮挡；‹ 无（tab 页无返回键）无碍；内容滚动正常、底部 tabbar 让位垫块不叠压；确认后迁移典籍库/我的 |
| 19 | tab 页迁移 + 顶栏实底（v0.3.11） | 典籍库/我的标题 = 解卦同高 6.4vh；三 tab 页滚动时内容不透出到标题栏带（顶栏实底）；深色模式栏带底色随翻转、标题可读；固定 3 页顶栏未动，顺检即可 |
| 20 | 底部 TabBar 实底（v0.3.12） | 跟随系统·浅色（最常用态）下 tab 栏不再透明：解卦/典籍库/我的滚动时文字不从栏后透出、栏内图标文字清晰；深色态、手动浅/深两态栏底同样不透明；栏顶边框线可见 |

---

## 结果记录

（跑完按项填：通过 / 不通过+现象+机型；不通过的转 优化方向.md 立案）
